(cfg_string (
  (name dirname)
  (prompt "Directory name")
  (default "dirname")
))

(cfg_string (
  (name filename)
  (prompt "File name")
  (default "filename")
))

(cfg_string (
  (name content)
  (prompt "Content")
  (default "Hello World!")
))