build: [
  ["dune" "subst"] {pinned}
  [
    "dune"
    "build"
    "-p"
    name
    "-j"
    jobs
    "@install"
    "@doc" {with-doc}
  ]
]
pin-depends: [
  ["dream-server" "git+https://github.com/tmattio/dream-serve.git"]
]
